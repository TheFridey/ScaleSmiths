# ScaleSmiths release candidate — 2026-07-13

Candidate: `rc-2026-07-13-00bc821-29df03f7`  
State: **BLOCKED — NOT READY FOR APPROVAL OR DEPLOYMENT**  
Base commit: `00bc8211ea65e44365d1cc52a9af215a0e847a5c` on `master`  
Payload: 716 files, SHA-256 `29df03f7d73ed06ef618152067f53b7cd147d1c384232a22dd98bb565ab19e5f`

This is a reproducible record of the upgraded platform payload, not an approved Forge deployment candidate. It must not be deployed automatically or manually while any blocker below remains. The machine-readable companion is [rc-2026-07-13.json](rc-2026-07-13.json).

## Decision

The candidate is blocked by two fundamental prerequisites: a clean dependency installation could not be completed, and the Docker Linux engine was unavailable. The clean attempts left both application dependency trees incomplete, so dependency-dependent checks were deliberately not run against stale or partial installations. The checkout also contains 272 uncommitted status entries; create the final commit and recompute all hashes before approval.

### Passed

- Environment-file hygiene and architecture-document consistency.
- Admin migration journal consistency: 42 journal entries and 42 SQL files.
- Production dependency audits: zero High or Critical findings in web and admin lockfiles.
- Docker Compose parsing for normal, host-Nginx, integration and release variants when required release variables were supplied.
- Seven fail-closed canary, Nginx-switch and rollback simulations.
- Candidate and migration payload hashing.

### Failed blockers

- Web `npm ci` timed out after ten minutes; an exact child npm process was terminated and `next` remained absent.
- Admin `npm ci` timed out after ten minutes with the same incomplete result.
- Web `npm ci --ignore-scripts` also timed out, ruling out lifecycle scripts as the primary cause.
- Node `22.12.0` is below the resolved `eslint-visitor-keys` engine requirement of `22.13.0`.
- Docker Desktop could not connect to `dockerDesktopLinuxEngine`.
- The payload is dirty and therefore is not identified by the base Git commit alone.

### Blocked checks

The following are **not passes**: migration from zero; migration from the previous production snapshot; unit and integration tests; Forge E2E; browser and visual tests; CodeQL, secret-history and container scans; web/admin image and production builds; deterministic Forge demo; generated-site sandbox, performance and accessibility runtime checks; immutable deployment-candidate verification; and release-gate verification.

The preceding production-readiness audit contains earlier successful results, but they do not substitute for regression evidence after a failed fresh install.

### Accepted non-blocking risks

- Both lockfiles report the known moderate PostCSS advisory inherited through Next.js. `npm audit` proposes a breaking and invalid major downgrade, so no forced audit fix was applied. High/Critical threshold remains clean.
- Live-provider Forge benchmarks, WebKit/Firefox expansion and scheduled infrastructure security jobs remain CI/scheduled evidence. They are not approval substitutes for the blocked hard gates above.

## Reproduction and required rerun

Use Node `>=22.13.0 <23`, npm from that Node release, a healthy Docker Linux engine, and a fresh clone or committed clean worktree. Verify the two package-lock hashes from the JSON manifest, then run:

```powershell
Set-Location web
npm ci --no-audit --no-fund
Set-Location ../admin
npm ci --no-audit --no-fund
Set-Location ..
npm run check:env-hygiene
npm run check:architecture-docs
node scripts/check-migrations.mjs
npm run test:integration
npm run test:forge-e2e
npm run test:release-simulation
```

Then run web `lint`, `test`, `build`, `test:e2e`, and `check:performance-budgets`; admin `lint`, `test`, `test:forge-benchmark`, `build`, `forge:demo -- --dry-run`, and the targeted sandbox/accessibility/release-gate suites. Run the repository Security and CodeQL workflows for secret-history, dependency review, container scanning, Dockerfile linting and SBOM evidence. Build both Dockerfiles and retain their immutable image digests.

For migration-from-previous testing, restore the latest verified production backup into a disposable, access-restricted PostgreSQL 16 database, record its migration journal/hash, apply web migrations and then admin migrations, and run schema/integration checks. Never point integration tooling at development or production; its URL safety validation intentionally rejects those targets.

## Required production environment

Required core values are `POSTGRES_DB`, `POSTGRES_USER`, `POSTGRES_PASSWORD`, `DATABASE_URL`, `NEXT_PUBLIC_SITE_URL`, `NEXT_PUBLIC_ADMIN_URL`, `PORTAL_SECRET`, `AUTH_SECRET`, `MFA_ENCRYPTION_KEY`, `ANALYTICS_CREDENTIAL_ENCRYPTION_KEY`, and `ADMIN_HEALTH_CHECK_TOKEN`.

Release operation additionally requires `SS_ENV_FILE`, `SS_GENERATED_SITES_DIR`, `SS_PRODUCTION_NETWORK`, and normally `SS_PUBLIC_HEALTH_URL`. Configure `RESEND_API_KEY`, `RESEND_FROM`, `SUPPORT_EMAIL`, and `ADMIN_PORTAL_URL` when email is enabled. Production Forge requires the budget variables and Docker sandbox variables in `.env.example`; provider keys are required only for enabled providers. `GIT_COMMIT_SHA` or `ERROR_MONITORING_RELEASE` must identify the approved commit/release. Placeholder values and long-lived bootstrap passwords are forbidden.

## Migration and backup instructions

1. Freeze writes or establish the documented consistent-backup window.
2. Record active release ID, Git commit, image digests, migration journals, database size and generated-workspace manifest.
3. Create encrypted, access-restricted backups of PostgreSQL, `.env`, host Nginx configuration, release state and `generated-sites`.
4. Restore the database and generated workspace into isolated infrastructure and prove the restore before proceeding. Record RPO, RTO and verification actor.
5. Confirm every migration is forward-only and backward-compatible with the retained application slot. Historical migration files must not be edited after production application.
6. Apply **web migrations first and admin migrations second**, never concurrently. Verify both journal tables and application health before traffic switching.

## Deployment instructions

Do not execute these until a replacement candidate is green, committed, hashed, represented by an approved immutable Forge deployment candidate, and all server-side release gates pass.

1. Follow [Canary release and rollback](../operations/canary-release-and-rollback.md).
2. Run `release-manager.mjs prepare --dry-run` with the approved candidate ID and inactive slot.
3. Run preparation without `--dry-run`; retain image digests, bounded logs and both internal health responses.
4. Obtain the required client and authorised release approvals.
5. Switch traffic manually. The manager must pass `nginx -t`, atomically update upstreams and preserve the previous healthy slot.
6. Observe logs, monitoring, budgets, jobs and user journeys for the agreed window. Do not prune the previous release or backup during that window.

## Rollback and post-deployment verification

Use `sudo -E node scripts/release-manager.mjs rollback --actor <identity>` only after confirming the previous application is compatible with the current database. If it is not, stop and restore the tested backup rather than improvising down migrations.

After release or rollback, verify public and admin health, TLS/Nginx, admin login and MFA, RBAC denial paths, portal client isolation, experience routing, quote/contact delivery, Forge read-only and queued-task views, release metadata, migration journals, reservation cleanup, email delivery, analytics ingestion, monitoring events and backup integrity.

## Release notes

This payload consolidates the ScaleSmiths upgrade programme: multi-user admin identity, MFA and central RBAC; structured logging and monitoring boundaries; explicit Forge quality/state/provenance/prompt registries; provider reliability and transactional AI budgets; sandbox, QA and controlled repair workflows; adaptive planning and structured review/evaluation stages; analytics and operations intelligence; immutable deployment candidates, release gates, and blue/green rollback tooling; expanded PostgreSQL, browser, visual, performance, benchmark and security CI coverage; and updated architecture, security and operations documentation.

## Final manual-approval checklist

- [ ] Commit the exact intended payload; recompute source, lockfile, migration and image hashes.
- [ ] Use Node `>=22.13.0 <23`; clean web and admin `npm ci` pass.
- [ ] Docker Linux engine is healthy; clean PostgreSQL and restored-previous-schema migrations pass.
- [ ] Unit, integration, Forge E2E, browser/visual, performance and accessibility checks pass.
- [ ] CodeQL, secret, dependency, Dockerfile, image and SBOM security evidence passes the documented thresholds.
- [ ] Web/admin production and container builds pass; generated-site sandbox and deterministic demo pass.
- [ ] Immutable Forge deployment candidate verifies the workspace and approved artifact hashes.
- [ ] All non-overridable release gates pass; every permitted override has owner, reason and compensating control.
- [ ] Backup restore is evidenced; migration, deployment, rollback and incident plans have named operators.
- [ ] Required production secrets are present in the secret store and placeholders/bootstrap credentials are absent.
- [ ] Authorised owner records final approval. No automated deployment is enabled.
