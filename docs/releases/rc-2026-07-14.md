# ScaleSmiths release candidate — 2026-07-14

Candidate: `rc-2026-07-14-e94e14f`  
State: **VALIDATED, BLOCKED FROM APPROVAL AND DEPLOYMENT**  
Base commit: `e94e14fe9727d4a275b538f75fd2f271e888d5b1` on `master`

The source and runtime failures found during the release regression have been corrected. This record is not a deployment approval: the validated fixes and generated evidence are not yet committed, and the previous-production-schema migration still requires an isolated restore of an actual production backup.

The machine-readable companion is [rc-2026-07-14.json](rc-2026-07-14.json). The generated SPDX SBOMs are [web](rc-2026-07-14-web.spdx.json) and [admin](rc-2026-07-14-admin.spdx.json).

## Corrected failures

- Upgraded both applications to patched Next.js `15.5.20`.
- Pinned the Node 22 Alpine base by immutable multi-platform digest and removed unused npm tooling from runtime images.
- Corrected Forge-generated animation code so generated TypeScript no longer compares impossible literal types.
- Updated the Forge workflow E2E diagnostics and lifecycle assertions to the implemented schema and activity names.
- Stabilised Playwright by mocking analytics ingestion and applying visual-noise controls after navigation.
- Added deterministic Chromium Linux baselines for desktop, tablet and mobile.
- Declared supported Next Image qualities, removing the production warning for quality 90.

## Passed evidence

- Clean Linux `npm ci` in both production Docker builds.
- Web: lint, 61 unit tests, production build, performance budgets, and 23 Chromium browser/visual checks.
- Admin: lint, 414 unit tests, production build, 10-fixture Forge benchmark, and deterministic Forge demo.
- PostgreSQL 16: migrations from empty database and 7 real integration tests, including concurrency and rollback.
- Full Forge workflow E2E, including controlled QA failure, repair, provenance, approvals and deployment blocking.
- Generated-site typecheck, lint, build, sandbox and accessibility/release-gate test coverage.
- Environment hygiene, architecture documentation, 42-entry admin migration consistency and 7 rollback/release simulations.
- Production dependency audits: zero High or Critical findings; the known upstream PostCSS advisory remains Moderate.
- Trivy on the exact final web and admin images: zero High or Critical OS or application findings.
- Hadolint on both Dockerfiles: pass.
- TruffleHog verified-secret history scan: zero verified or unverified findings.
- SPDX JSON SBOM generated for each final image.

## Immutable runtime evidence

- Web image: `sha256:f5c061f61acf16282f67eb69a41e5d3884a136e5c2e26ff820dd707282b53f24`
- Admin image: `sha256:7bcd93aa784ddb86be515353d33881a3dc7bf882636a9a1e1614a394039ca305`
- Node base: `node:22-alpine@sha256:16e22a550f3863206a3f701448c45f7912c6896a62de43add43bb9c86130c3e2`

## Remaining approval blockers

1. Commit the exact reviewed payload and regenerate the candidate record from that clean commit. A dirty working tree cannot be an immutable deployment candidate.
2. Restore the latest verified production PostgreSQL backup into an isolated PostgreSQL 16 environment, apply web migrations followed by admin migrations, and record the journal/schema comparison. No production backup is available in this checkout, so this evidence cannot be fabricated locally.
3. Let the protected-branch CodeQL and dependency-review jobs pass for the final commit. Local equivalents passed where available, but GitHub-native checks remain required evidence.
4. Record human release approval after confirming production secrets, backup ownership, monitoring connectivity and the maintenance window.

## Required production environment

Core values are `POSTGRES_DB`, `POSTGRES_USER`, `POSTGRES_PASSWORD`, `DATABASE_URL`, `NEXT_PUBLIC_SITE_URL`, `NEXT_PUBLIC_ADMIN_URL`, `PORTAL_SECRET`, `AUTH_SECRET`, `MFA_ENCRYPTION_KEY`, `ANALYTICS_CREDENTIAL_ENCRYPTION_KEY`, and `ADMIN_HEALTH_CHECK_TOKEN`.

Release operation additionally requires `SS_ENV_FILE`, `SS_GENERATED_SITES_DIR`, `SS_PRODUCTION_NETWORK`, and normally `SS_PUBLIC_HEALTH_URL`. Configure the Resend variables when email is enabled, Forge budgets and sandbox values when Forge execution is enabled, and only the provider keys for enabled providers. `GIT_COMMIT_SHA` or `ERROR_MONITORING_RELEASE` must identify the approved commit.

## Migration, deployment and rollback

1. Freeze writes or establish a consistent-backup window. Record the active release, image digests and both migration journals.
2. Create encrypted backups of PostgreSQL, environment configuration, host Nginx state and generated workspaces; prove the restore in isolated infrastructure.
3. Apply web migrations first and admin migrations second. Verify journals, schema and health before traffic switching.
4. Follow [Canary release and rollback](../operations/canary-release-and-rollback.md). Prepare the inactive release with `--dry-run`, then prepare it for real only after the immutable Forge candidate and server-side gates pass.
5. Test Nginx configuration before the atomic traffic switch and retain the previous healthy release.
6. Roll back with the documented release manager only when the previous app remains compatible with the migrated database; otherwise restore the tested backup.

No production deployment was performed.

## Manual approval checklist

- [ ] Commit and review this exact payload; regenerate source and image hashes from the clean commit.
- [ ] Pass restored-previous-production migration verification.
- [ ] Pass required GitHub CodeQL and dependency-review checks.
- [ ] Verify required secrets contain no placeholders or bootstrap credentials.
- [ ] Verify backup restore evidence, monitoring delivery and named rollback operator.
- [ ] Create and verify the immutable Forge deployment candidate.
- [ ] Confirm every non-overridable release gate passes and all approvals are current.
- [ ] Record authorised owner approval. Do not enable automatic deployment.
