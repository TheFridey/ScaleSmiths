# ScaleSmiths release candidate — 2026-07-20

Candidate: `rc-2026-07-20-ab459cc`
State: **BLOCKED — A TECHNICAL GATE FAILS; NOT AN APPROVAL**
Base commit: `ab459cc5661b6c2e3451c4e5568ee78bb22d0923` on `test/host-nginx-integration`

This record regenerates the release evidence from the current **clean** working tree and
supersedes [rc-2026-07-14](rc-2026-07-14.md). It is not a deployment approval: a
non-overridable technical gate fails, several evidence artifacts are unavailable in this
checkout, and the required GitHub-native and human production checks are outstanding.

The machine-readable companion is [rc-2026-07-20.json](rc-2026-07-20.json). Every payload
hash in it was freshly computed from this commit and differs from the previous candidate;
image digests and SBOMs are labelled **unavailable** rather than reused.

## Why this candidate is blocked

The admin production dependency audit **fails**:

```
cd admin && npm audit --omit=dev --audit-level=high   → exit 1 (1 high, 0 critical, 4 moderate)
```

The High finding is in **undici `<= 6.26.0`** (admin pins `undici 6.21.3`, added with the
outbound SSRF client): advisories GHSA-f269-vfmq-vjvj, GHSA-vrm6-8vpv-qv8q,
GHSA-v9p9-hfj2-hcw8 and GHSA-vxpw-j846-p89q (WebSocket parser overflow, unbounded
decompression and denial of service). The SSRF client uses only the HTTP `Agent`, not the
WebSocket client, but `npm audit` gates on the package regardless. **Remediation:** bump
admin `undici` to a patched release (`> 6.26.0`) and regenerate this candidate.

## Immutable payload hashes (this commit)

| Artifact | SHA-256 |
| --- | --- |
| Node base digest (from both Dockerfiles) | `sha256:16e22a550f3863206a3f701448c45f7912c6896a62de43add43bb9c86130c3e2` |
| web `package-lock.json` | `eba3096fa2781efef8b3f17b462fa9a77c498ca530c598d68135b2db09827253` |
| admin `package-lock.json` | `02bcaaf0180d5da2597d5fa3e0bb2c3fedf0f98f34b08c1b6549c9d9bd7eb0f9` |
| web `Dockerfile` | `32a7ca42ac2a1cf2edb044d51186154b5e17ca5ec426ca7f91745257505cb7ef` |
| admin `Dockerfile` | `252edb688970d648618d72823765205dc815f36d01774e02f71d5bca1ad1143e` |
| `.env.example` | `368bb155cdea95d652fe665d4a55fb97b51d3ac74383fbf5271da46b2a43221f` |
| final web image digest | **unavailable** (image not built this session) |
| final admin image digest | **unavailable** (image not built this session) |
| web SPDX SBOM + hash | **unavailable** (syft not present; image not built) |
| admin SPDX SBOM + hash | **unavailable** (syft not present; image not built) |

Per-source-file integrity is anchored by the content-addressed commit SHA above.

## Locally executed evidence (real results, this checkout)

- **Passed:** admin lint; web unit tests (108); admin unit tests (499); migration
  verification (web 13/13, admin 46/46); migration history integrity (52 historical + 7
  forward locked); migration history and consistency tests; release and rollback
  simulation; dependency governance (check + test); production topology (check + test);
  environment hygiene; architecture documentation; GitHub Actions policy (check + test);
  backup-migration verifier safeguards; **web** production npm audit (0 high, 0 critical, 3
  moderate).
- **Failed:** **admin** production npm audit (1 high — see above).

## Required GitHub-native checks (not a local substitute)

CodeQL; dependency-review; web lint (not locally executable here — `eslint` is not installed
under `web/node_modules`); web and admin production builds; web Chromium journeys and visual
regression; cross-browser Firefox/WebKit smoke; web performance budgets; admin Forge
benchmark; full Forge workflow E2E; the PostgreSQL empty-migration and integration database
job (24 integration tests passed at this HEAD during development, but the CI run is the
required evidence for the final commit); TruffleHog, Hadolint, Trivy and SBOM workflows.

## Unavailable evidence (labelled, never inferred)

Final web/admin image digests; web/admin SPDX SBOMs and hashes; Trivy final-image scans;
Hadolint; TruffleHog; and **restored-production-backup migration verification** — no
production PostgreSQL backup exists in this checkout, so this cannot be produced locally.

## Human production checks (outstanding)

Verify production secrets contain no placeholders or bootstrap credentials; verify backup
restore evidence, ownership and retention; verify monitoring delivery; name the rollback
operator; confirm the maintenance window; record authorised owner approval. Do not enable
automatic deployment.

## Migration, deployment and rollback

Apply web migrations first and admin migrations second; verify both journals, schema and
health before switching traffic. Prepare the inactive release with `--dry-run` first, test
the host Nginx configuration before the atomic switch, and retain the previous healthy
release. Roll back with the documented release manager only when the previous app remains
compatible with the migrated database; otherwise restore the tested backup. The production
checkout root is `/var/www/scalesmiths/ScaleSmiths` and the admin origin is
`https://admin.scalesmiths.co.uk`. See [Canary release and
rollback](../operations/canary-release-and-rollback.md).

No production deployment was performed.

## Approval checklist

- [ ] Remediate the admin High-severity undici audit finding and regenerate this candidate.
- [ ] Merge to `master`; pass required GitHub CodeQL and dependency-review checks.
- [ ] Generate and record final image digests, SPDX SBOMs, Trivy, Hadolint and TruffleHog
      evidence for the exact images.
- [ ] Pass restored-previous-production migration verification.
- [ ] Verify required secrets contain no placeholders or bootstrap credentials.
- [ ] Verify backup restore evidence, monitoring delivery and named rollback operator.
- [ ] Confirm every non-overridable release gate passes and all approvals are current.
- [ ] Record authorised owner approval. Do not enable automatic deployment.
