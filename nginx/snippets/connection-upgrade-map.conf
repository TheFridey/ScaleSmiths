# Shared between production (nginx/host-scalesmiths.conf) and the disposable test
# harness (tests/nginx). http context. Maps the Upgrade request header to the
# Connection value Nginx must send upstream for websocket support.
map $http_upgrade $connection_upgrade {
  default upgrade;
  "" close;
}
