import type { Metadata } from "next"

export const quoteThanksMetadata: Metadata = {
  title: "Quote Request Received",
  robots: { index: false, follow: false },
  alternates: { canonical: "/quote/thanks" },
}
